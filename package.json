{
  "name": "markdown",
  "version": "1.0.0",
  "main": "index.js",
  

  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-client": "cd frontend && npm install",
    "install-server": "cd backend && npm install",
    "start-client": "cd frontend && npm run dev",
    "start-server": "cd backend && npm run start",
    "install-all": "npm-run-all install-client install-server",
    "start-all": "npm-run-all --parallel start-client start-server",
    "setup-and-start": "npm-run-all install-all start-all"
    
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
